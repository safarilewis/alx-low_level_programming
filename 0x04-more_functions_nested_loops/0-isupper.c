#include "main.h"
/**
 *_isupper - checks whether something is uppercase
 *@c: variable
 *Return: Always zero
 */
int _isUpper(int c)
{
if (c >=m 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}}
